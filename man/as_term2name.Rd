% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_term2name.R
\name{as_term2name}
\alias{as_term2name}
\title{Export GO term names as TERM2NAME}
\usage{
as_term2name(go, minGSSize = NULL, maxGSSize = NULL)
}
\arguments{
\item{go}{A \code{GO} or \code{GOSubgraph} object.}

\item{minGSSize}{Minimum number of genes per GO term.}

\item{maxGSSize}{Maximum number of genes per GO term.}
}
\value{
A \code{data.frame} with columns \code{term} and \code{name}.
}
\description{
Returns a two-column \code{data.frame} suitable for
\code{clusterProfiler::enricher(TERM2NAME = ...)}.

The returned table is restricted to GO terms present in the current
\code{GO} or \code{GOSubgraph} object.

Optional size filtering can be applied based on the number of mapped
genes per GO term.
}
\examples{
data("go_cc_ecoli", package = "GOcontext")

# Export GO term names for the current graph
term2name <- as_term2name(go_cc_ecoli)
head(term2name)

# Apply size filtering based on mapped genes
term2name_filtered <- as_term2name(
    go = go_cc_ecoli,
    minGSSize = 5,
    maxGSSize = 100
)
head(term2name_filtered)

}
